apiVersion: v1
kind: Service

metadata:
  name: order-service
  namespace: ecom

  labels:
    app: order-service

spec:
  type: ClusterIP

  selector:
    app: order-service

  ports:
    - name: http
      protocol: TCP
      port: 8086
      targetPort: 8086
