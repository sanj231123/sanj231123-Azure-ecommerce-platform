apiVersion: v1
kind: Service

metadata:
  name: product-service
  namespace: ecom

  labels:
    app: product-service

spec:
  type: ClusterIP

  selector:
    app: product-service

  ports:
    - name: http
      protocol: TCP
      port: 8083
      targetPort: 8083
