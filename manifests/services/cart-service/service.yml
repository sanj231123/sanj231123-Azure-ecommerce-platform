apiVersion: v1
kind: Service

metadata:
  name: cart-service
  namespace: ecom

  labels:
    app: cart-service

spec:
  type: ClusterIP

  selector:
    app: cart-service

  ports:
    - name: http
      protocol: TCP
      port: 8085
      targetPort: 8085
